name: Deploy to Staging

on:
  push:
    branches:
      - staging
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Staging Environment
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run linter
        run: npm run lint || echo "Linting completed with warnings/errors (non-blocking)"
        continue-on-error: true

      - name: Run tests
        run: npm test
        continue-on-error: true

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production
          NEXT_TELEMETRY_DISABLED: 1

      - name: Deploy to staging server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          script: |
            set -e
            cd /var/www/staging
            
            echo "📥 Pulling latest changes from staging branch..."
            git fetch origin staging
            git reset --hard origin/staging
            
            echo "📦 Installing dependencies..."
            npm ci --legacy-peer-deps --production=false || {
                echo "⚠️ npm ci failed, trying npm install..."
                npm install --legacy-peer-deps
            }
            
            # Verify Next.js is available
            if [ ! -f "node_modules/.bin/next" ]; then
                echo "❌ Next.js not found. Installing..."
                npm install next --save
            fi
            
            echo "🔨 Building application..."
            npm run build || {
                echo "❌ Build failed. Checking for issues..."
                ls -la node_modules/.bin/ | grep next || echo "Next.js binary not found"
                exit 1
            }
            
            echo "🗄️ Running database migrations..."
            npx prisma generate
            npx prisma migrate deploy || echo "⚠️ Migration failed, continuing..."
            
            echo "🔄 Restarting application with PM2..."
            if pm2 list | grep -q "jewellery-staging"; then
              pm2 restart jewellery-staging
            else
              pm2 start ecosystem.config.js
            fi
            
            echo "✅ Staging deployment completed!"
            
            # Wait a moment and check if the app is running
            sleep 5
            pm2 status

      - name: Health check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_SSH_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          script: |
            echo "🏥 Performing health check..."
            sleep 10
            curl -f https://staging.adorneluxejewels.com/api/health || echo "⚠️ Health check failed, but deployment completed"
